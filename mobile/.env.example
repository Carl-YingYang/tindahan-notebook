# Copy to .env for DEV-time defaults only (never commit real values).
# The in-app Suki AI → Settings screen always takes precedence, and keys
# entered there are stored only in the device's local SQLite database.
EXPO_PUBLIC_GLM_API_KEY=
EXPO_PUBLIC_GLM_BASE_URL=
EXPO_PUBLIC_GLM_MODEL=
